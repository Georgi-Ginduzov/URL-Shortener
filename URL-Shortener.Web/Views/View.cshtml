@model IEnumerable<URL_Shortener.Web.Models.UrlVMs.UrlReadOnlyVM>

@{
    ViewData["Title"] = "View";
}

<h1>View</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ShortenedUrl)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TargetUrl)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Analitycs)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ShortenedAt)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ExpiresAt)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ShortenedUrl)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TargetUrl)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Analitycs)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ShortenedAt)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ExpiresAt)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
